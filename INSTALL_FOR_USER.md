# Инструкция по установке Text Analyzer

## Требования
- Python 3.11 или выше (рекомендуется Python 3.13)
- Операционная система: macOS, Windows, или Linux

## Установка (5-10 минут)

### Шаг 1: Установите Python

**macOS:**
```bash
# Проверьте версию Python
python3 --version

# Если Python не установлен, установите с https://www.python.org/downloads/
```

**Windows:**
1. Скачайте Python с https://www.python.org/downloads/
2. При установке отметьте "Add Python to PATH"

**Linux:**
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

### Шаг 2: Распакуйте файлы

Распакуйте архив `text-analyzer.zip` в удобное место.

### Шаг 3: Откройте терминал в папке проекта

**macOS/Linux:**
```bash
cd /путь/к/text_analyzer
```

**Windows:**
- Откройте папку в проводнике
- Нажмите Shift + правая кнопка мыши
- Выберите "Открыть окно PowerShell здесь" или "Открыть терминал здесь"

### Шаг 4: Создайте виртуальное окружение

```bash
python3 -m venv venv
```

**Windows:**
```powershell
python -m venv venv
```

### Шаг 5: Активируйте виртуальное окружение

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```powershell
venv\Scripts\activate
```

### Шаг 6: Установите зависимости

```bash
pip install -r requirements.txt
```

⏳ Это займет 2-5 минут. Будет загружено ~100MB файлов.

### Шаг 7: Запустите приложение

**macOS/Linux:**
```bash
./run.sh
```

**Windows:**
```bash
streamlit run src/app.py
```

**Или вручную:**
```bash
streamlit run src/app.py
```

### Готово! 🎉

Приложение откроется в браузере автоматически по адресу: `http://localhost:8501`

---

## Использование

1. Выберите язык текста (Русский или Беларуская)
2. Загрузите файл (.txt, .pdf или .docx)
3. Просмотрите результаты анализа

---

## Остановка приложения

Нажмите `Ctrl + C` в терминале или закройте окно терминала.

---

## Следующий запуск

После первоначальной установки, для запуска приложения:

1. Откройте терминал в папке проекта
2. Активируйте окружение:
   - macOS/Linux: `source venv/bin/activate`
   - Windows: `venv\Scripts\activate`
3. Запустите: `./run.sh` (macOS/Linux) или `streamlit run src/app.py` (Windows)

---

## Возможные проблемы

### "command not found: streamlit"

**Решение:**
```bash
source venv/bin/activate  # Или venv\Scripts\activate на Windows
pip install -r requirements.txt
```

### "No module named 'streamlit'"

**Решение:**
Убедитесь, что виртуальное окружение активировано (должно быть `(venv)` в начале строки терминала).

### Порт 8501 занят

**Решение:**
```bash
streamlit run src/app.py --server.port 8502
```

---

## Поддержка

При возникновении проблем свяжитесь с отправителем приложения.


